'use client';

// Gallery hidden globally — re-enable once high-quality images are ready.
// To restore: uncomment the full component body from git history (TourGallery.tsx).

interface TourGalleryProps {
  images: string[];
  tourName: string;
}

export default function TourGallery({ images: _images, tourName: _tourName }: TourGalleryProps) {
  return null;
}
